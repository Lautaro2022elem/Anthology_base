[logic]
active = sr_idle

[sr_idle]
on_actor_inside = sr_idle@teleport %=disable_ui =run_postprocess(black:3543)%

[sr_idle@teleport]
on_info = %=run_postprocess(controller_hit)%
on_game_timer = 0 | sr_idle %=teleport_actor(agro_lc_to_shval_walk:agro_lc_to_shval_look) =run_postprocess(stc_fade_out)%